# Tests

Automated tests will be added as modules become real. The initial shell uses
small PowerShell checks under `scripts/` because the first acceptance target is
process startup and rendering behavior.

